# ============================================
# plantilla de variables de entorno - labmedical backend
# ============================================
# copiar este archivo como .env para desarrollo local:
#   cp .env.example .env
#
# para produccion, copiar como .env.production:
#   cp .env.example .env.production
#
# generar secretos seguros con:
#   python scripts/generate_secrets.py

# base de datos postgresql
# local: postgresql://postgres:postgres@localhost:5432/labmedical
# neon.tech: postgresql://user:pass@ep-xxxx.neon.tech/labmedical?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/labmedical

# servidor
PORT=3000
RUST_LOG=info

# jwt secret (en produccion generar con scripts/generate_secrets.py)
JWT_SECRET=your-secret-key-min-32-characters-long

# almacenamiento de imagenes (cloudflare r2 o aws s3)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_REGION=us-east-1
AWS_S3_BUCKET=labmedical-products

# email (resend.com - gratis 3000/mes)
EMAIL_API_KEY=re_your_api_key
EMAIL_FROM=onboarding@resend.dev
EMAIL_TO=your-email@example.com

# cors (origenes permitidos separados por coma)
CORS_ORIGIN=http://localhost:4321